pipeline {
    agent any

    environment {
        REGISTRY = "docker.io/miatrika05/ticklab"
        IMAGE_TAG = "${env.BUILD_NUMBER}"
        DEPLOY_USER = "miatrika"
        DEPLOY_HOST = "192.168.100.50"
        DEPLOY_PATH = "/var/www/ticklab"
    }

    options {
        timestamps()
        buildDiscarder(logRotator(numToKeepStr: '20'))
        disableConcurrentBuilds()
    }

    stages {
        stage('Checkout') {
            steps { checkout scm }
        }

        stage('Build') {
            steps {
                script {
                    load "${env.WORKSPACE}/jenkins/stages/build.groovy"
                }
            }
        }

        stage('Test & Quality') {
            parallel {
                stage('Unit Tests') {
                    steps {
                        ansiColor('xterm') {
                            script { load "${env.WORKSPACE}/jenkins/stages/test.groovy" }
                        }
                    }
                }
                stage('Static Analysis') {
                    steps {
                        ansiColor('xterm') {
                            script { load "${env.WORKSPACE}/jenkins/stages/qa.groovy" }
                        }
                    }
                }
                stage('Security Scan') {
                    steps {
                        ansiColor('xterm') {
                            script { load "${env.WORKSPACE}/jenkins/stages/security.groovy" }
                        }
                    }
                }
            }
        }

        stage('Test Credentials') {
            steps {
                script {
                    echo "🧪 Vérification des credentials"
                    withCredentials([usernamePassword(credentialsId: 'dockerhub-creds', usernameVariable: 'U', passwordVariable: 'P')]) {
                        echo "✅ Credentials trouvés pour utilisateur: ${U}"
                    }
                }
            }
        }

        stage('Package & Push') {
            steps {
                ansiColor('xterm') {
                    script {
                        echo "=== Étape Package & Push démarrée ==="
                        sh "docker build -t ${REGISTRY}:${IMAGE_TAG} ."
                        echo "✅ Build terminé"

                        withCredentials([usernamePassword(credentialsId: 'dockerhub-creds', usernameVariable: 'DOCKERHUB_USER', passwordVariable: 'DOCKERHUB_PASS')]) {
                            echo "🔑 Tentative de connexion Docker Hub pour l'utilisateur : ${DOCKERHUB_USER}"
                            sh '''
                                echo $DOCKERHUB_PASS | docker login -u $DOCKERHUB_USER --password-stdin || true
                                echo "✅ Connexion Docker terminée (même si échec ignoré)"
                                docker info | grep -A3 "Username" || echo "Pas connecté"
                            '''
                            echo "🏷️  Tagging de l'image..."
                            sh "docker tag ${REGISTRY}:${IMAGE_TAG} ${REGISTRY}:latest"
                            echo "📦 Package terminé (push désactivé)"
                        }
                        echo "=== Fin du stage Package & Push ==="
                    }
                }
            }
        }


        stage('Deploy') {
            steps {
                ansiColor('xterm') {
                    script { load 'jenkins/stages/deploy.groovy' }
                }
            }
        }
    }

    post {
        success { echo "✅ Pipeline completed successfully." }
        failure { echo "❌ Pipeline failed — check logs." }
        always {
            sh 'docker container prune -f || true'
            sh 'docker image prune -f || true'
        }
    }
}
